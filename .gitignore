# Python
__pycache__/
*.py[cod]
*.egg-info/
.env
venv/
.venv/

# Streamlit (solo ignorar secretos, no el config de tema)
.streamlit/secrets.toml

# Claude Code local settings
.claude/settings.local.json

# OS
.DS_Store
Thumbs.db
desktop.ini

# LaTeX build artifacts
docs/_build/
docs/*.aux
docs/*.log
docs/*.out
docs/*.toc

# Carpeta antigua con emoji (reemplazada por assets/)
🖼️ Assets/

# Informes generados (output, no codigo) — viven aparte del repo
informes_apa_2026/
Informe_Consolidado_*.pdf

# ─── Pipeline de informes APA — NO va al repo publico ─────────────────────────
# Este repo contiene solo el simulador. Los scripts de lote y los datos de
# escuelas (nombres, coordenadas, veredictos) viven en local / OneDrive.
batch_scall.py
generar_bd_mapa.py
generar_consolidado.py
run_*.py
escuelas_*.csv
escuelas_*.geojson

# Config local de Claude Code (rutas de esta maquina)
.claude/
